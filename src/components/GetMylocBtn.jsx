import React from 'react'

const GetMylocBtn = (props) => {
  return (
       <button className='btn' onClick={props.onClick} >

              Get My Location
            </button>
  )
}

export default GetMylocBtn
